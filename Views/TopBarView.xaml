<UserControl x:Class="FaceRecognitionApp.Views.TopBarView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:FaceRecognitionApp.Views"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <Grid>

        <Rectangle Fill="{StaticResource bluishColor}"/>

        <Button Command="{Binding NavigateToDashBoard}">
            <Button.Template>
                <ControlTemplate>
                    <Image Source="{StaticResource homeIcon}"
                           HorizontalAlignment="Left"
                           Width="25"
                           Margin="10 0"/>          
                </ControlTemplate>
            </Button.Template>
        </Button>

        <Image Source="{StaticResource optionsIcon}"
               Width="25"/>
    </Grid>
</UserControl>
